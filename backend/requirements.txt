# Phase 2 backend — pinned to the environment the Phase 1 model was serialised in.
# scikit-learn MUST stay at 1.8.0: final_model.joblib was pickled with it, and a
# version change can silently alter unpickling or predict_proba behaviour.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
numpy>=1.26
joblib>=1.3
scikit-learn==1.8.0

# tests
pytest>=8.0
anyio>=4.0
httpx>=0.27

# NOTE: do NOT add tensorflow. Phase 1 applied a parsimony rule and deployed the
# sklearn model; the GRU is not used.
